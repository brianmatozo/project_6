services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "3000:3000"
    secrets:
      - db-password
    environment:
      - NODE_ENV=development
      - MYSQL_HOST=mysql
      - MYSQL_PORT=3306
      - MYSQL_USER=root
      - MYSQL_DATABASE=mydb
      - JWT_SECRET=9b12b3a5f53adeebf0846d70dec3f254b26def5e0ef56da53030dc8c05e3c652
      - EMAIL_USER=battlefield.elcapo@gmail.com
      - EMAIL_PASS=eynl uopt pesu svlh
      - EMAIL_FROM=noreply@example.com
    volumes:
      - ./frontend/dist:/app/frontend/dist
      - ./package.json:/app/package.json
      - ./bun.lockb:/app/bun.lockb
    depends_on:
      - mysql
    networks:
      - app-network
  mysql:
    image: mysql:latest
    container_name: mysql
    restart: always
    secrets:
      - db-password
    environment:
      - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/db-password
      - MYSQL_DATABASE=mydb
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - app-network
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    networks:
      - app-network

volumes:
  db_data:

networks:
  app-network:
    driver: bridge

secrets:
  db-password:
    file: db/password.txt
